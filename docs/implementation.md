# 实现与测试说明

## 技术与平台核实

- 语言：CommonJS JavaScript，零运行时依赖。
- 绘制：单 Canvas 2D，所有规则采用格点坐标，屏幕缩放只由绘制和输入模块处理。
- 构建：静态扫描本工程的相对 require 引用，输出微信与浏览器两个独立包，不下载远程代码。
- 自动测试：Node 内置 test；自建 CDP 驱动真实 Edge，仅用于测试本工程。
- 本机工具：Node 24.14.0、npm 11.9.0、微信开发者工具 Stable 2.02.2608070、Edge 152.0.4191.66。
- 微信开发文档域名在本次检索中无法打开，未宣称已核实在线所有接口。微信官方开源的[平台适配说明](https://wechat-miniprogram.github.io/minigame-canvas-engine/tutorial/platform.html)支持将触摸、绘制等能力放入平台层；当前微信接口还需在真实开发者工具中验证。
- 微信入口使用 createCanvas、getWindowInfo（旧接口回退 getSystemInfoSync）、getMenuButtonBoundingClientRect、onTouchStart/Move/End/Cancel、onHide/onShow、getStorageSync/setStorageSync、createInnerAudioContext 和 vibrateShort。音效震动失败不阻断游戏。

## 移动与并行

箭头路径从尾到头存储。头部沿原朝向无限延伸，一个保持身体长度的窗口沿该折线前移。渲染裁切在棋盘边界；尾部与线条厚度均离开后触发完成。

占用依据当前身体位置更新，出口射线对其他已经接受的移动保留到完成。临时移动冲突提示等待，不扣生命；静态阻挡才是有效错误。使用累计游戏时间计算绝对距离，避免逐帧累加距离漂移。

## 随机生成

逆向加入新箭头：每次加入时出口相对当前布局畅通，身体不重叠、不自交且不挡住自身出口。随机过程控制长度、转弯和填充率。

独立验证器重新扫描全部出口并逐层消除，不依赖构造顺序。满足难度阈值后才交付玩家；重试预算耗尽使用同档已验证固定候选。每 40 次候选尝试让出执行机会，避免长时间占据游戏主循环。

1000 个种子的验证不只检查“有解”标记：独立解法还逐条送入实际 Session，直到实际通关。另有 30 个高难种子的并行移动测试，逐帧检查是否有重复占用。

## 存档

保存完整原布局与版本、已移除集合、生命、教学标记、设置和进度。动画中保存时，将已接受的移动写成完成后的稳定快照，前台画面仍等尾部出界才更新计数。

两份存档包含内容校验。主份损坏使用备份；双份损坏但原布局可读时，保守恢复原关卡初始状态，不虚增通关。暂时读取失败时禁止默认进度覆盖旧存档，重试先重新读取。

切后台停止帧循环及声音，整理移动、保存、暂停。恢复不补算后台时间。失败为不可逆的本轮终态，进行中的动画完成不能再次改成胜利。

## 参数现状

| 阶段 | 关卡 | 棋盘 | 填充率 | 最低遮挡层数 |
| --- | --- | --- | --- | --- |
| 初见 | 1（教学） | 6×6 | 35%—50%（教学例外） | 1 |
| 寻路 | 2 | 8×8 | 50%—63% | 2 |
| 深锁 | 3 起 | 14×14 | 100% | 10 |

第 21 关起每关 3 次机会、12 格/秒移动、200 毫秒阻挡反馈。这些是开发基线，待手机试玩调整。难度提升不等于合法点击顺序能造成新死锁：移除合法箭头只减少静态遮挡。

## 测试修复记录

1. P2 首轮暂停断言发现逐帧累加距离的浮点偏差。保留严格断言，修复为累计时间计算绝对距离，重跑通过。
2. 本地 Edge 启动遇到资料文件短暂占用与 GPU 子进程失败。增加短暂占用重试，采用受控测试进程的软件绘制配置后，真实浏览器测试能够运行；未跳过浏览器测试。
3. 审查中补充损坏箭头对象的校验和读取失败不能覆盖原存档的保护，各自添加回归测试。

未删除测试、未将失败用例改成 skip，也未降低原验收断言。格式化使用本机 TypeScript 打印器，不引入新的项目依赖；格式化后重新运行全量测试。


## 试玩调整：第3关直接高难度与重置进度（2026-09-11）
本条取代此前分阶段提速方案。第1关教学、第2关8×8，第3关及以后直接使用12×12最高档，填充75%—85%，至少5层遮挡，初始可移除比例不超过50%。生命仍从第21关引入。难度配置版本为3，已有棋盘保留原布局；下一关或重置进度后使用新配置。
首页或暂停菜单进入设置，可点击重置关卡进度。先确认，可取消；确认后清空当前棋盘和解锁进度、回到第1关、重新启用教学与生命说明，保留音效和震动偏好，同时保存主存档与备份。保存失败显示重试提示。

## 满棋盘挑战（2026-09-11，取代此前最高档参数）
第1、2关保持入门；第3关起12×12棋盘的144个格点全部由箭头占用，不重叠。每关至少24条箭头，开局恰好1条可移除，至少10层遮挡。箭头长度2—9格，保留视觉间隙与原有点击容差。采用从满棋盘逐步取出箭头的反向生成方式，每一步都有合法出口；独立求解器验证全部可消除后才能交付。生成失败使用同标准已验证保底布局。生成器版本2、难度配置版本4；已有棋盘不变，下一关或主页重置进度后生效。生命仍从第21关开始扣除。


## 密度调整（2026-09-12，取代12×12挑战棋盘）
第3关起使用14×14满棋盘，共196格，比原144格增加约36%；每关至少36条箭头，保持开局仅1条可移除、至少10层遮挡。第1、2关和生命规则不变。难度配置版本5；已有存档保留原棋盘，下一关或主页重置后生效。小屏及手机尺寸验证每条箭头头部与轻微偏移点击均可选中。


## 次数限制调整（2026-09-12，取代第21关起扣次数）
用户确认：第1、2关不限次数；第3关起每关3次容错。点到被挡住的静止箭头扣1次，第3次点错立即失败；正确消除、空白点击和暂时被移动箭头挡住均不扣次数。失败可重试，恢复3次。已有第3关及以后无限次数存档在读取时启用3次，保留布局与消除进度；已有扣次存档保留剩余次数，重载不能补次数。难度配置版本6。

## 递增棋盘与精确点击（2026-09-12，当前规则）
第1、2关保持入门；第3—7关14×14，第8—12关16×16，第13—17关18×18，第18关及以后20×20。当前上限20×20，避免无限缩小导致难以辨识。所有挑战档铺满，开局仅1条可移除，每关3次点错机会；最低遮挡层数依次10、12、14、16。
游戏页右上角提供放大按钮：点击到2倍，再点击到3倍，此时按钮显示全图可恢复。放大后单指拖动移动视野；拖动、缩放、取消触摸及区域外点击均不得触发消除或扣次数。点击按缩放和移动后的棋盘坐标判断，绘制裁剪在棋盘框内。新关卡恢复全图。旧存档保留布局，下一关或重置后使用新尺寸。生成器版本3、难度配置版本7。

## 障碍物与递减限时（2026-09-12，当前规则）
第15关开始固定石块：15—19关1个，20—24关2个，25—29关3个，30关起4个。石块占格、不能消除，碰撞判断和独立求解均考虑永久阻挡；生成时保证全部箭头仍可消除，满棋盘按箭头与石块合计计算。
第20—24关180秒，此后每5关减少10秒，50关起保持120秒。正确消除不扣机会；点错3次或倒计时归零失败。棋盘生成和说明弹窗不计时，暂停及后台冻结。失败重试恢复本关完整时间和3次机会。进度存档保留剩余时间和失败原因；重载不补时间。第15、20关有新规则说明，HUD显示倒计时，最后30秒变红。旧存档保留原棋盘与计时规则，新关卡使用新规则。生成器版本4，难度配置版本8。

## 独立挑战模式（2026-09-12）
主页与普通闯关并列显示挑战模式入口，无需解锁。每局直接采用第30关档：20×20满棋盘、4块石块、3次容错，时间固定90秒。生成完成后显示规则说明，点击开始挑战才计时；支持放大拖动、暂停及后台冻结。失败重试同一布局并恢复90秒与3次机会，胜利可再挑战一张随机棋盘。
普通闯关和挑战相互独立。进入挑战前保存普通关卡，挑战不会解锁或覆盖普通进度；退出至主页或重新打开游戏结束本轮挑战，回到普通进度。声音与震动偏好共用。挑战设置中不显示重置普通进度入口。

## 挑战解锁及三种道具（2026-09-12，取代挑战无门槛入口）
挑战模式在普通闯关通关第19关、解锁第20关时开启，并弹出一次解锁提示；未达到时主页标示20关解锁。提示确认状态随存档保存，重置普通进度会重新锁定。
普通闯关及挑战每局免费提供各1次：限时关加时30秒、有限次数关容错+1、重排剩余箭头。道具入口在游戏页左上方，道具菜单暂停计时；未限时或不限次数时对应道具不可消耗。已失败或通关不允许使用；正在移动时需等待动画结束。
重排保留剩余箭头数量、累计消除进度、石块数量、剩余时间与容错次数；从独立求解结果中保留剩余部分并复验可解。生成不足或失败时旋转当前可解布局作为保底。异步期间锁定操作，不重复扣道具。存档记录道具消耗，重载不补道具；重试本关恢复原始完整棋盘和每种道具1次。挑战返回首页结束本局，普通闯关存档独立保留。


## 2026-09-13：40关、数量门、钥匙门和每日领奖（覆盖上述旧规则）

普通关卡共40关，总览每页20关，通关20关解锁第二页。第2关仍为25×25高密度棋盘；21关起采用竖向长方形棋盘。普通非教学关保留密集长箭头、单初始出口和高遮挡深度，所有布局由独立求解器验证。

前10关每关300秒；11关起每关缩短4秒，到40关180秒。第4关首次出现4个永久障碍，之后每关增加1个，到40关40个。第4、8、12关是简单教学关，采用较少箭头和更浅的消除顺序，下一关恢复高难度。教学弹窗、暂停及后台不计时。

第8关引入数量门，移出指定数量的普通箭头后自动打开，门面显示还需数量；钥匙箭头不计入普通箭头数量。第12关引入钥匙门，钥匙箭头有专属颜色和钥匙图标，完全移出后打开对应颜色的门。13关起组合两种门，后续逐渐增加钥匙门。重排保留开门进度和钥匙对应关系，不产生死局。教学说明可点击按钮或空白区域关闭。

首通奖励改在4n−1关（n为正整数），即3、7、11、15、19、23、27、31、35、39关；每次加时1个、容错2个、重排1个。已领取关卡不重复发奖。主页新增每日奖励按钮，每天免费领取加时、容错、重排各1个，领取后显示已领取；北京时间零点换日，与每日分享各5个的奖励分别计算。每次通关奖励、每日奖励和分享奖励都展示带道具图标及数量的物品弹窗，关闭不重复发奖。

保留现有解锁进度、道具库存和领取记录；正在玩的旧棋盘保留原布局，倒计时按新上限减去已用时迁移一次，新开关卡使用新布局。重置进度不重置每日领取额度。竞速仍通关4关解锁、3关、无道具不限错不限时；每日引用5/10/15，每周改为5/15/25，避免将新手教学关当成周赛高难关。配置变更隔离旧竞速布局成绩。


2026-09-13 闸门与教学加强：第4关14×16、34条箭头；第8关18×20、56条箭头；第12关20×22、70条箭头。教学保留多层遮挡，提供持续变化的阶段提示。第8关起改为封住一段出口的闸门，每扇至少影响5条箭头，先清理可用区域才能开门；第12关的钥匙需先解开多层遮挡。第13关起组合数量门与钥匙门。开门有900毫秒收起动画、提示音和新出口箭头高亮。保留原进度与道具库存；已经保存的旧棋盘仍可继续，回总览重新进入关卡即可体验新版布局。
