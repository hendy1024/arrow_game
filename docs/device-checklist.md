# 微信与真机验收清单（待执行）

用户已提供 AppID wx47e5456f2f4f5ade，并指定 iPhone 15 Pro；用户安排稍后导入。Android 设备尚未指定。

开发者工具本次无法自动初始化：CLI 需要写入其用户目录的 .cli 文件，但权限工具仅返回读权限。因此未将微信开发者工具实际编译记录为通过。

## 导入与编译

1. 导入项目根目录，确认类型为小游戏、AppID 正确，使用 dist/wechat/game.js。
2. 编译并查看控制台，记录开发者工具和基础库版本。
3. 确认首页、开始、教学、通关、暂停及设置没有错误。
4. 预览并扫码，使用有权限的微信账号。

## iPhone 15 Pro

- 顶部状态区、微信胶囊和底部手势区不遮挡游戏。
- 第一关理解箭头头部方向、身体跟随和阻挡含义。
- 点击头部、线段、转角都准确；轻扫和双指不误扣生命。
- 第 21 关或以上高密度棋盘仍看得清、点得准。
- 多条箭头连续点击无穿越、重复计数或无响应。
- 声音及震动开关生效；切后台停止声音。
- 移动中切后台、结束进程再进入，棋盘和生命恢复一致。
- 生命耗尽失败，同布局重试；清空通关后可继续。
- 记录至少 90 帧动画的持续帧率及点击反馈时间，保留视频/性能面板证据。

## Android

在真实 Android 手机执行同样清单，记录型号、系统、微信版本。至少一台测试设备应能代表可获得的较低性能档；若 iPhone 与 Android 都属于高性能设备，不应宣称覆盖了低性能设备。

## 调参确认

根据实际试玩确认：生命从哪关开始、每关次数、密度和点击容差、移动速度。将实际结论回写 config 和 PRD，再回归受影响测试。

`reports/device-validation.json` 为待填记录，不是测试通过证明。只填写实际执行的结果；`npm run verify:devices` 会检查是否仍有缺项。未完成时保留失败退出码。
